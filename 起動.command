#!/bin/zsh
# ============================================
# 読書ツール 起動用スクリプト
# ============================================

# スクリプトのディレクトリに移動
cd "$(dirname "$0")"

echo "📚 読書ツールを起動しています..."
echo "----------------------------------------"

# 実行
./run.sh
